<div align="center">

# ⏱️ Родительский контроль — Android Screen Timer

<img src="poster.jpg" alt="Родительский контроль — Android Screen Timer" width="100%">

### Телевизор, планшет или телефон — экранное время под контролем.

**Бесплатный семейный ограничитель времени для Android**<br>
с кодом родителя, напоминаниями, выбором приложений, маскировкой и защитой от удаления.

**Не подберёт код. Не отключит лимит через обычные настройки. Device Owner блокирует обычное удаление приложения.**

[![CI](https://github.com/alexaistudio/parental-control-android-screen-timer/actions/workflows/ci.yml/badge.svg)](https://github.com/alexaistudio/parental-control-android-screen-timer/actions/workflows/ci.yml)
[![Latest release](https://img.shields.io/github/v/release/alexaistudio/parental-control-android-screen-timer?sort=semver&display_name=tag&label=версия)](https://github.com/alexaistudio/parental-control-android-screen-timer/releases/latest)
[![Downloads](https://img.shields.io/github/downloads/alexaistudio/parental-control-android-screen-timer/total?label=скачиваний)](https://github.com/alexaistudio/parental-control-android-screen-timer/releases)
[![License](https://img.shields.io/badge/лицензия-PolyForm_Strict_1.0.0-2563eb)](LICENSE.md)

![Android](https://img.shields.io/badge/Android-6.0%2B-3DDC84?logo=android&logoColor=white)
![Android TV](https://img.shields.io/badge/Android_TV-поддерживается-3DDC84)
![Google TV](https://img.shields.io/badge/Google_TV-поддерживается-4285F4?logo=google&logoColor=white)
![Tablet](https://img.shields.io/badge/планшеты-поддерживаются-2563eb)
![Phone](https://img.shields.io/badge/телефоны-поддерживаются-7c3aed)
![Controls](https://img.shields.io/badge/управление-сенсор_%7C_пульт-f59e0b)
![Languages](https://img.shields.io/badge/языки-RU_%7C_EN-7c3aed)
![Offline](https://img.shields.io/badge/работает-локально-059669)
![No ads](https://img.shields.io/badge/реклама-нет-16a34a)
![No tracking](https://img.shields.io/badge/слежка-нет-16a34a)
![Two APKs](https://img.shields.io/badge/установка-2_APK_в_одном_релизе-2457D6)
![Java](https://img.shields.io/badge/Java-17-ED8B00?logo=openjdk&logoColor=white)
![Target SDK](https://img.shields.io/badge/target_SDK-35-3DDC84)

[**⬇️ Скачать готовый APK**](https://github.com/alexaistudio/parental-control-android-screen-timer/releases/latest) ·
[Как включить максимальную защиту](#максимальная-защита-от-удаления-device-owner) ·
[💚 Поддержать разработку](#поддержать-разработку) ·
[🇬🇧 Full English README](README.en.md)

</div>

---

## Ребёнок засмотрелся? Таймер не забудет.

Один раз задайте правила — Android Screen Timer сам считает реальное время просмотра на телевизоре, планшете или телефоне, предупреждает ребёнка и закрывает выбранные приложения, когда лимит закончился.

- ⏱️ **Задайте дневной лимит** — на всё устройство или только на YouTube, игры и другие выбранные приложения.
- 🔐 **Код родителя с телефона действует до 5 минут** — его спокойно можно ввести с пульта или экрана, а потом он уже не подходит. Резервный PIN хранится отдельно.
- 👨‍👩‍👧‍👦 **Добавьте несколько родителей** — один QR-код подключает второй и следующие телефоны.
- ➕ **Временный код добавляет ровно заданное время** — от 10 минут до 2 часов. Постоянный PIN открывает синий режим родителя с ручным выбором 10, 15, 20, 30, 40, 60, 90 или 120 минут.
- 🔔 **Таймер напомнит, если ребёнок засмотрелся** — через 10, 20 или 30 минут появится простой вопрос: продолжить или закончить.
- 🥷 **10 вариантов имени и значка** — Android Screen Timer, Калькулятор, Медиа-служба, Часы, Погода, Заметки, Календарь, Файлы, Галерея или Справка.
- 🖼️ **Узнавайте выбранные приложения сразу** — рядом с каждым пунктом показывается его настоящая иконка.
- 🛡️ **Ребёнок не сможет просто удалить ограничитель** — защита обычных системных настроек включена по умолчанию, но её можно отдельно отключить; экраны установки/удаления и Device Owner остаются самостоятельными уровнями защиты.
- ✅ **Настройки не мешают обычному выходу** — после работы родителя кнопка `Назад` ведёт прямо на главный экран; защита появляется только на действительно открытой опасной системной странице и не зависает на `Проверка…`.
- 🔌 **Безопаснее с USB** — обычные флешки, зарядка и периферия ничего не отключают. Только файл `Recovery` переводит тот же экран в режим родителя без удаления настроек; USB Recovery можно полностью выключить.
- 🕵️ **Скрытый вход остаётся под контролем родителя** — удержание `Назад` и семь нажатий в углу можно полностью отключить отдельной галочкой, независимо от USB Recovery.
- 📴 **Никаких аккаунтов и слежки** — без рекламы, аналитики, телеметрии и облака.
- 📲 **Установка с телефона без Bugjaeger** — отдельный «Родительский установщик» находит устройство в локальной сети, сопрягается по системному коду Android, устанавливает встроенный APK и проверяет включение контроля.
- ✅ **Работает с прошивками, которые молчат после установки** — установщик проверяет фактическую версию приложения на устройстве и продолжает настройку, даже если OEM Package Manager не закрыл поток и не прислал финальную строку `Success`.
- 🧾 **Если что-то не сработало — ответ устройства не потеряется** — установщик постоянно сохраняет команды, ответы и полные ошибки; сверху отдельно показывает IP/порт цели и итог установки, а весь журнал позволяет скопировать или сохранить в TXT.
- 🌍 **Русский и English** — язык можно переключить с первого запуска, в настройках и прямо на экране кода.

> Максимальная защита от удаления работает в режиме Device Owner. Без него Android не разрешает обычному приложению гарантированно запретить собственное удаление; маскировка меняет плитку, но не системный список приложений.

## English

### Free family screen-time control for Android TV, tablets, and phones

Set the rule once. Android Screen Timer tracks real viewing time, shows what remains, reminds your child to take a break, and blocks the device or selected apps when the daily limit is over.

- **A phone code works for up to 5 minutes**, so it is easy to enter with a remote or touchscreen, and then it expires.
- Set a daily limit for the whole device or selected apps.
- Connect the same private QR setup to several parents’ phones.
- A temporary phone code adds the parent's preset 10–120 minutes; the permanent PIN opens manual choices for 10, 15, 20, 30, 40, 60, 90, or 120 minutes.
- Show a clear “keep watching or finish” reminder every 10, 20, or 30 minutes.
- Choose from 10 discreet names and icons.
- Protect Android settings and the package installer with the parent code; Device Owner can block uninstalling at the Android system level.
- No ads, accounts, analytics, telemetry, or cloud storage.
- Switch between Russian and English from the first screen, settings, and every parent-code panel.

[**Download the latest APK**](https://github.com/alexaistudio/parental-control-android-screen-timer/releases/latest)

[Read the complete installation, protection, privacy, and build guide in English](README.en.md).

---

«Родительский контроль — Android Screen Timer» — локальное приложение для Android TV, Google TV, планшетов и телефонов. Оно считает активное экранное время, показывает остаток в правом верхнем углу и закрывает контролируемые приложения после исчерпания дневного лимита.

## Возможности

- Полный интерфейс на русском и английском; компактный переключатель языка доступен с первого экрана, в настройках, выборе приложений и полноэкранных окнах кода/блокировки.
- Уникальная для каждой установки QR-привязка к Google Authenticator, Microsoft Authenticator, Aegis или другому TOTP-приложению; секрет остаётся только на контролируемом устройстве и телефоне родителя.
- Действующий QR можно повторно открыть в настройках и отсканировать на нескольких телефонах родителей; подключённые ранее телефоны продолжают работать.
- Каждый шестизначный код меняется через 30 секунд, но принимается устройством до пяти минут. Список старых кодов не хранится: проверяются только десять ограниченных временных окон.
- Резервный PIN родителя из 4–8 цифр; в настройках хранится только PBKDF2-хеш с индивидуальной солью.
- Один дневной лимит от 1 до 1440 минут для всего устройства или выбранных приложений.
- Выбор приложений открывается отдельным экраном; Back/«Назад» сразу сохраняет галочки и возвращает на компактную страницу настроек.
- Настройки и полноэкранное меню родительского кода ограничены безопасной шириной, элементы и PIN-клавиатура уменьшены, а фокус больше не раздвигает интерфейс за край экрана.
- Если Accessibility не включается, приложение открывает карточку именно своей службы, объясняет Android 13+ Restricted Settings и сохраняет диагностический журнал. Весь журнал можно показать постраничными QR-кодами без PIN, TOTP-секрета и истории просмотра.
- Мобильный установщик постоянно записывает этапы обнаружения, сетевые проверки, ADB-команды, полные ответы и stack trace в приватный журнал до 512 КиБ. Вверху отдельным блоком сохраняются IP и порт цели, успешное сопряжение/подключение, передача 100% APK, ожидание Package Manager, подтверждённая версия, итог `Success` или ошибка. Журнал на телефоне отображается текстом, копируется целиком и сохраняется как `.txt`; QR-страницы оставлены только в диагностике самого ограничителя на TV/другом целевом устройстве. Код сопряжения, приватный ключ и APK-бинарник не записываются.
- В окне выбора рядом с названием и package ID отображается настоящая иконка приложения.
- Изменение лимита с сенсорного экрана или пульта кнопками `−15`, `−1`, `+1`, `+15`, без экранной клавиатуры.
- Обратный отсчёт поверх активного приложения.
- Шестизначный временный код автоматически добавляет установленное родителем время от 10 до 120 минут. Постоянный PIN переводит тот же блокирующий экран в синий режим родителя и показывает ручной выбор 10, 15, 20, 30, 40, 60, 90 или 120 минут.
- Режим родителя вызывается удержанием `Назад` восемь секунд на пульте либо семью нажатиями в левом верхнем углу сенсорного экрана. Это не второй overlay: текущий экран меняет цвет и назначение, поэтому родительский интерфейс не может оказаться под блокировкой. Оба скрытых жеста вместе полностью отключаются отдельной галочкой; тогда настройки открываются через значок приложения и постоянный PIN.
- После первоначальной настройки создаётся одноразовый четырёхзначный аварийный код. Он хранится только как PBKDF2-хеш, после успешного применения удаляется, а три неверные попытки блокируют его перебор на 30 минут. Новый код создаётся в настройках.
- По желанию каждые 10, 20 или 30 минут появляется простое детское напоминание: «Ты уже смотришь N минут. Продолжить?» Кнопка «Нет, закончить» возвращает на главный экран, а подтверждение не повторяется после перезапуска.
- Учёт приостанавливается при выключенном экране и заставке; состояние подтверждённо записывается каждые 5 секунд и переживает резкое отключение питания с потерей не более последнего интервала.
- Служба автоматически возвращается после загрузки устройства, если Android сохранил её включённой.
- USB-восстановление реагирует только на пустой или непустой файл `Recovery`/`Recovery.txt`/`File Recovery` в корне смонтированной флешки, без учёта регистра. Оно открывает синий режим родителя и ничего не удаляет. Обычные USB-устройства и зарядка игнорируются; функцию можно выключить отдельной галочкой.
- Защита обычных системных настроек кодом включена по умолчанию и отключается отдельной галочкой, если семье нужен свободный доступ к настройкам. Экраны установки/удаления защищаются отдельно; на поддерживаемых прошивках можно дополнительно включить системного администратора устройства.
- Device Owner-режим системно запрещает удаление пакета. Временный доступ и USB Recovery не снимают Device Owner и не стирают конфигурацию.
- Значок launcher можно переключать между 10 встроенными профилями: «Android Screen Timer», «Калькулятор», «Медиа-служба», «Часы», «Погода», «Заметки», «Календарь», «Файлы», «Галерея» и «Справка». Это маскировка значка, а не замена системной защиты.
- Ручная проверка GitHub Releases: ASCII-прогресс загрузки, сверка SHA-256, package ID, версии и сертификата подписи APK перед запуском установщика.
- Нет аккаунта, рекламы, аналитики, телеметрии и облака. Сеть используется только после нажатия «Проверить обновление» для официального репозитория `alexaistudio/parental-control-android-screen-timer`.

## Совместимость и ограничения

Минимальная версия — Android 6.0 / API 23. Проект собирается с Android SDK 35. Интерфейс адаптируется к телевизору, планшету и телефону и работает с сенсорным экраном, пультом, мышью или клавиатурой.

Без root или режима Device Owner стороннее приложение не может гарантированно запретить собственное удаление. В обычном режиме Android Screen Timer закрывает системные настройки и установщик PIN/TOTP-экраном, а Device Admin добавляет системный шаг деактивации. Device Owner включает настоящий системный запрет удаления пакета. Safe mode, сброс устройства и особенности OEM всё равно нужно проверять на конкретной модели. Android Screen Timer не меняет системные пакеты, не запускает Activity из `BOOT_COMPLETED` и не использует циклы перезапуска, поэтому сам по себе не должен создавать boot loop.

USB Recovery — дополнительный, а не гарантированный системный путь: доступ приложения к корню съёмного накопителя зависит от прошивки Android. Создайте в корне флешки пустой файл `Recovery`, `Recovery.txt` или `File Recovery`; регистр букв и размер не важны. При распознавании файла откроется режим родителя без сброса PIN, лимитов, приложений или Device Owner. На устройствах без USB используйте постоянный PIN или одноразовый аварийный код.

## Установка

В каждом релизе два подписанных файла:

- `AndroidScreenTimer-1.4.7.apk` — сам ограничитель для телевизора, планшета или телефона;
- `AndroidScreenTimer-Parent-1.4.7.apk` — родительский установщик для Android-телефона; внутри уже находится первый APK.

### Вариант 1 — установить с телефона

1. Установите `AndroidScreenTimer-Parent-1.4.7.apk` на телефон родителя. Bugjaeger, компьютер и root не нужны.
2. Подключите телефон и целевое устройство к одной обычной Wi‑Fi сети без изоляции клиентов.
3. На целевом **телевизоре / Google TV** откройте `Настройки → Система` или `Настройки устройства → Об устройстве` и нажмите 7 раз `Сборка`, `Номер сборки` либо `Android TV OS build`. Вернитесь в `Для разработчиков`.
4. На целевом **телефоне / планшете** откройте `Настройки → О телефоне/планшете → Номер сборки` и нажмите его 7 раз. Затем откройте `Система → Для разработчиков`.
5. Включите `Беспроводная отладка` и откройте `Сопряжение с помощью кода`. Не закрывайте это окно: в нём показаны IP, временный **порт сопряжения** и шестизначный код. Введите их в установщик и нажмите `Сопрячь`. Это требуется один раз.
6. Закройте окно с кодом и вернитесь на главный экран `Беспроводной отладки`. В строке `IP-адрес и порт` указан другой, рабочий **порт подключения**. Введите его и нажмите `Подключить`.
7. После сообщения `Подключено` нажмите `INSTALL`. Установщик передаст встроенный APK, разрешит Restricted Settings, добавит службу контроля без удаления TalkBack, проверит фактический результат и откроет Android Screen Timer.
8. Кнопка `Найти по Wi‑Fi / сети` только помогает заполнить адреса автоматически. Если роутер блокирует обнаружение, ручной ввод IP и двух портов работает без поиска.
9. По умолчанию установщик выключит современную беспроводную отладку после успеха. Старую сетевую отладку некоторых прошивок нужно выключить вручную.

Не смешивайте способы: `USB debugging` не включает Wi‑Fi ADB. Если на устройстве нет `Беспроводной отладки`, но есть старая `Сетевая отладка` / `ADB over network`, включите её и используйте показанный IP/порт (часто `5555`) только как порт подключения — код сопряжения обычно не нужен. Если сетевой отладки нет совсем, используйте прямую установку ниже.

### Вариант 2 — установить APK напрямую

Это отдельный USB/локальный путь. Обычный кабель телефон ↔ телевизор чаще всего не подходит, потому что оба разъёма работают как USB-host. Используйте USB-флешку либо компьютер, к которому целевое устройство действительно подключается как ADB-устройство.

1. Возьмите `AndroidScreenTimer-1.4.7.apk` из [GitHub Releases](https://github.com/alexaistudio/parental-control-android-screen-timer/releases).
2. Установите его с флешки, из браузера или через ADB:

   ```powershell
   adb install -r AndroidScreenTimer-1.4.7.apk
   ```

3. Откройте Android Screen Timer и отсканируйте первый QR-код приложением-аутентификатором на телефоне родителя.
4. Задайте резервный PIN, лимит и область контроля.
5. Подтвердите использование службы специальных возможностей.
6. Если переключатель службы сразу возвращается в `Выкл.`, нажмите в Android Screen Timer `Открыть «О приложении»`. Если прошивка показывает меню `⋮ → Разрешить ограниченные настройки`, включите его и попробуйте снова. На некоторых телевизорах этого пункта нет; тогда используйте «Родительский установщик» и ADB-путь выше либо следуйте правилам конкретной прошивки.
7. В открывшихся системных настройках включите «Контроль времени Android Screen Timer».
8. Если служба всё равно выключается, откройте `Журнал ошибок в виде QR` и отсканируйте телефоном все пронумерованные страницы.
9. Вернитесь в приложение и убедитесь, что статус службы — «включена».

### Максимальная защита от удаления (Device Owner)

Этот режим предназначен для Android-устройства, которое можно подготовить как управляемое. На уже используемом профиле с аккаунтами Android обычно отклоняет назначение Device Owner; тогда нужен сброс/чистый профиль. После установки APK и до добавления аккаунтов выполните:

```powershell
adb shell dpm set-device-owner dev.tvtimer.app/.TimerDeviceAdminReceiver
adb shell dpm list-owners
```

Откройте Android Screen Timer: в разделе защиты должен появиться статус `Device Owner: удаление Android Screen Timer системно запрещено`. USB Recovery не снимает Device Owner: файл `Recovery` только открывает родительский режим, не разрушая защиту и настройки.

Маскировка выбирается после входа родителя в настройках приложения. Android не разрешает установленному APK принимать произвольное имя или значок во время установки, поэтому используются 10 заранее встроенных безопасных launcher-профилей. В системном списке приложений пакет всё равно виден; некоторым оболочкам нужен перезапуск главного экрана, чтобы сразу обновить значок.

После обычной перезагрузки Android повторно подключает ранее включённую службу специальных возможностей. Если устройство имеет отдельную настройку автозапуска или энергосбережения, разрешите в ней работу Android Screen Timer.

## Сборка

Нужны JDK 17 и Android SDK с Platform 35 и Build Tools 35.0.0. Android Studio подходит. SDK не хранится в репозитории; Gradle wrapper хранится.

В подготовленной локальной рабочей папке SDK скопирован в исключённый из Git каталог `.android-sdk`, а `scripts/verify.ps1` подхватывает его автоматически. После чистого клонирования укажите установленный SDK через `ANDROID_HOME`/`ANDROID_SDK_ROOT` или поместите его в `.android-sdk`.

PowerShell:

```powershell
$env:ANDROID_HOME='C:\Android\Sdk'
.\gradlew.bat testDebugUnitTest
.\gradlew.bat lintDebug
.\gradlew.bat assembleDebug
```

Linux/macOS:

```bash
export ANDROID_HOME="$HOME/Android/Sdk"
chmod +x gradlew
./gradlew testDebugUnitTest lintDebug assembleDebug
```

Полная локальная проверка в PowerShell:

```powershell
.\scripts\verify.ps1
```

Результат — `app/build/outputs/apk/debug/app-debug.apk`. Это APK с отладочной подписью, подходящий для локальной установки. Для публичного релиза нужен отдельный закрытый release-keystore; ключи и пароли нельзя добавлять в репозиторий.

Локальная release-сборка читает путь к keystore, alias и пароли из исключённого Git-файла `signing/release.properties`. Эти же значения можно переопределить переменными окружения `ANDROID_SCREEN_TIMER_KEYSTORE_FILE`, `ANDROID_SCREEN_TIMER_KEYSTORE_PASSWORD`, `ANDROID_SCREEN_TIMER_KEY_ALIAS` и `ANDROID_SCREEN_TIMER_KEY_PASSWORD`. Без всех четырёх значений `assembleRelease` намеренно завершается ошибкой, чтобы unsigned/debug APK не был случайно опубликован как стабильный релиз.

## Данные и разрешения

Настройки, выбранный язык, TOTP-секрет, хеш PIN и дневной счётчик находятся только в приватном `SharedPreferences` приложения и исключены из backup/переноса. TOTP-коды и QR не отправляются в сеть. Приложение запрашивает:

Мобильный установщик хранит на телефоне родителя отдельный приватный диагностический файл до 512 КиБ. В нём есть локальные IP-адреса, модель устройства, команды и ответы ADB, но нет кода сопряжения, приватного ADB-ключа или бинарных данных APK. Ничего не отправляется автоматически: копирование и сохранение выполняются только после нажатия родителя.

- `RECEIVE_BOOT_COMPLETED` — подготовить дневное состояние после загрузки без запуска окон или циклов;
- `INTERNET` — только ручная проверка и загрузка релиза из `github.com`/GitHub API;
- `REQUEST_INSTALL_PACKAGES` — передать уже проверенный APK системному установщику после отдельного согласия пользователя;
- службу специальных возможностей — видеть только имя пакета активного окна и показывать accessibility overlay;
- видимость запускаемых TV-приложений через узкие `<queries>` — заполнить список выбора.

Чтение содержимого окон, Usage Access и обычное разрешение «поверх других приложений» не используются.

Подключение USB намеренно считается аварийным ключом и полностью удаляет локальную конфигурацию. После этого приложение нужно настроить заново.

## Структура и проверка

Описание компонентов и потока данных находится в [docs/ARCHITECTURE.md](docs/ARCHITECTURE.md), а сравнение с существующими решениями и платформенные основания — в [docs/RESEARCH.md](docs/RESEARCH.md).

Перед первым коммитом можно включить версионированные privacy-hooks:

```powershell
.\scripts\setup-hooks.ps1
```

CI выполняет privacy-проверку, unit-тесты, Android Lint и debug-сборку.

## Поддержать разработку

Android Screen Timer остаётся бесплатным для семьи. Если приложение оказалось полезным, разработку можно добровольно поддержать через **USDT TRC-20**.

**Сеть:** TRON<br>
**Адрес:** `TMoM4t1JsevXo42cRBiYue51NXrsjuGhqd`

<p align="center">
  <img src="docs/usdt-trc20-qr.png" alt="QR-код адреса USDT TRC-20 в сети TRON" width="240">
</p>

QR-код содержит только указанный выше адрес. Перед отправкой убедитесь, что в кошельке выбрана сеть **TRON (TRC-20)**.

## Лицензия

Проект распространяется по условиям [PolyForm Strict License 1.0.0](LICENSE.md). Допускается немодифицированное некоммерческое использование; официальные условия лицензии имеют приоритет над кратким описанием.

Уведомления и лицензии библиотек мобильного ADB-установщика перечислены в [THIRD_PARTY_NOTICES.md](THIRD_PARTY_NOTICES.md).
